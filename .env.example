# Environment variables for development
# Copy to .env and fill in values

# Vault Configuration
VAULT_ADDR=http://127.0.0.1:8200
VAULT_ROLE_ID=
VAULT_SECRET_ID=

# Database (for local dev only - production uses Vault)
DATABASE_URL=postgresql://vulnscanner:devpassword@localhost:5432/vulnscanner

# Metasploit (for local dev only)
MSF_PASSWORD=

# Grafana
GRAFANA_PASSWORD=admin

# Database password (for docker-compose)
DB_PASSWORD=devpassword
